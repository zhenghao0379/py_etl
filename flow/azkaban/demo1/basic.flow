nodes:
  - name: jobC
    type: noop
    # jobC depends on jobA and jobB
    dependsOn:
      - jobA
      - jobB

  - name: jobA
    type: command
    config:
      command: sh /data/demo/test.sh ${job_param}

  - name: jobB
    type: command
    config:
      command: pwd
